describe('Retry Proxy', () => {

  it('Only traps method calls');
  it('Only retries failures matching the predicate');
  it('Retries the failure');
  it('Retries the failure up to <times> times');

});
